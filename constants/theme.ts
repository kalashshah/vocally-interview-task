import { Theme } from "../types/theme";

const light: Theme = {
  backgroundColor: "white",
  textColor: "black",
};

const dark: Theme = {
  backgroundColor: "black",
  textColor: "white",
};

export { light, dark };
