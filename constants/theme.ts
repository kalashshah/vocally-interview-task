import { Theme } from "../types/theme";

const light: Theme = {
  backgroundColor: "white",
  textColor: "black",
  primaryRed: "#e32f45",
};

const dark: Theme = {
  backgroundColor: "black",
  textColor: "white",
  primaryRed: "#e32f45",
};

export { light, dark };
