export type Theme = {
  backgroundColor: string;
  textColor: string;
  primaryRed: string;
  secondaryBackground: string;
  secondaryTextColor: string;
};

export enum ThemeMode {
  light = "light",
  dark = "dark",
}
